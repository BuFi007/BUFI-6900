{
  "name": "@bufi/playground",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "BUFI-6900 playground: a fork of Circle's circle-smart-account example (Vite + React, passkey owner) pointed at the local sandbox, with BUFI panels that drive the ERC-6900 plugins through @bufi/modular-wallets-core — plus the headless e2e drive.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "e2e": "bun run scripts/e2e.ts",
    "check:type": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.app.json"
  },
  "dependencies": {
    "@bufi/modular-wallets-core": "workspace:*",
    "react": "19.2.7",
    "react-dom": "19.2.7",
    "viem": "2.45.3"
  },
  "devDependencies": {
    "@types/bun": "1.3.10",
    "@types/react": "19.2.17",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "4.3.4",
    "typescript": "5.8.2",
    "vite": "5.4.18"
  }
}
